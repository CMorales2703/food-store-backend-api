[phases.setup]
nixPkgs = ["jdk21"]

[phases.build]
cmds = [
    "chmod +x gradlew",
    "./gradlew build -x test --no-daemon"
]

[start]
cmd = "java -jar build/libs/food-store-backend-api-0.0.1-SNAPSHOT.jar"
